version: "3.9"

services:
  redis:
    image: redis:7.0
    container_name: redis
    ports:
      - "6380:6379"
      
  qdrant:
    image: qdrant/qdrant
    container_name: qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
